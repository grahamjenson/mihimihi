- if @event.id != nil
  - url = admin_event_path(@event)
- else
  - url = admin_events_path

= form_for @event, :url => url do |f|
  - if @event.errors.any?
    #error_explanation
      %h2= "#{pluralize(@event.errors.count, "error")} prohibited this event from being saved:"
      %ul
        - @event.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :content
    = f.text_area :content
  .field
    = f.label :years_ago
    = f.text_field :years_ago
  .field
    = f.label :over_time
    = f.text_field :over_time
  .field
    = f.label :lonlat
    = f.text_area :lonlat
  
  .field
    = f.label :large_icon_url
    = f.text_field :large_icon_url

  .actions
    = f.submit 'Save'
